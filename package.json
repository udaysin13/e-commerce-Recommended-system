{
  "name": "ecommerce-recommendation-system",
  "private": true,
  "scripts": {
    "dev": "start \"backend\" cmd /k \"cd /d backend && npm run dev\" && npm --prefix frontend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev"
  }
}
